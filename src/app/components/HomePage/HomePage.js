import React from 'react';
import debug from 'debug';

debug('kammy:Homepage.jsx');

export default () => (
  <div id="home-page">
    <banner className="header">
      <h1>Home page!</h1>
    </banner>
  </div>
);
